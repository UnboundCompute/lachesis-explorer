'use client'
import { CodeBlock } from './CodeBlock'
export function InstallView({onUpload}:{onUpload:()=>void}) { return <main className="install-layout"><div className="install-copy"><span className="install-glyph"><span>↗</span></span><span className="panel-label">THE READER, ON YOUR MACHINE</span><h2>Trace your own codebase.</h2><p>Run the Lachesis CLI locally. It emits the same bundle this demo renders — no server, no model, no guessing.</p><button type="button" className="primary-button" onClick={onUpload}>Load a bundle.json <span><span>↗</span></span></button></div><div className="install-code"><span className="code-label">INSTALL</span><CodeBlock>{'npx lachesis init\nlachesis trace --repo . --out bundle.json'}</CodeBlock><span className="code-label">MCP CONFIG</span><CodeBlock>{'{\n  "mcpServers": {\n    "lachesis": {\n      "command": "npx",\n      "args": ["lachesis-mcp"]\n    }\n  }\n}'}</CodeBlock></div></main> }
