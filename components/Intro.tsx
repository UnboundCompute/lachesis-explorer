import type { App } from '../lib/lachesis'
export function Intro({view,app,notice}:{view:'trace'|'journey'|'install';app:App;notice:string}) { const copy={trace:['Follow a value through the graph.','See exactly where data comes from and where it goes — across aliases, calls, and indirection.'],journey:['Walk one request, end to end.','A focused call path answers how the code actually works, hop by hop.'],install:['Put the reader on your codebase.','Lachesis is a deterministic code-graph MCP. This demo is its cached output.']}[view]; return <section className="intro"><div><p className="eyebrow">DETERMINISTIC CODE UNDERSTANDING</p><h1>{copy[0]}</h1><p className="lede">{copy[1]}</p>{notice&&<p className="import-notice" role="status">{notice}</p>}</div><div className="commit"><span>ACTIVE REPOSITORY</span><strong>{app.language || 'unknown'} · {app.commit || 'uncommitted'}</strong><span>{app.lines.toLocaleString()} lines indexed · {app.flows.length} flows</span></div></section> }
