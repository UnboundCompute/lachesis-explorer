import type { App } from '../lib/lachesis'
export function Intro({view,app,notice}:{view:'trace'|'journey'|'install';app:App;notice:string}) { const copy={trace:['Trace one value. See every handoff.','Follow origins, transformations, aliases, and sinks with source evidence attached.'],journey:['Walk the request as the code sees it.','Inspect a focused callpath from entrypoint to effect, one grounded hop at a time.'],install:['Bring deterministic code evidence into your workflow.','Build the graph locally, query it over MCP, and inspect the same bundle here.']}[view]; return <section className="context-strip"><div className="context-copy"><span className="context-kicker">{view==='trace'?'VALUE-FLOW LENS':view==='journey'?'REQUEST-PATH LENS':'LOCAL WORKFLOW'}</span><h1>{copy[0]}</h1><p>{copy[1]}</p></div><dl className="bundle-facts"><div><dt>Repository</dt><dd>{app.name || 'Untitled'}</dd></div><div><dt>Language</dt><dd>{app.language || 'Unknown'}</dd></div><div><dt>Indexed</dt><dd>{app.lines.toLocaleString()} lines</dd></div><div><dt>Revision</dt><dd>{app.commit || '—'}</dd></div></dl>{notice&&<p className="import-notice" role="status">{notice}</p>}</section> }
