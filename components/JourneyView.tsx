'use client'
import type { App } from '../lib/lachesis'
import { trackEvent } from '../lib/analytics'
import { PathCanvas, type PathItem } from './PathCanvas'
import { NodeInspector } from './NodeInspector'
import { EvidencePanel } from './EvidencePanel'
type Props={app:App;entryIndex:number;setEntryIndex:(v:number)=>void;hopId:string;setHopId:(v:string)=>void;inspectorOpen:boolean;onInspectorOpen:()=>void;onInspectorClose:()=>void}
export function JourneyView({app,entryIndex,setEntryIndex,hopId,setHopId,inspectorOpen,onInspectorOpen,onInspectorClose}:Props){const entry=app.entries[entryIndex]??app.entries[0];if(!entry)return <section className="workspace-empty"><h2>No request paths in this bundle</h2><p>Value flows are still available. Callpaths appear here when the bundle includes them.</p></section>;const selected=app.nodes.find(node=>node.id===hopId)??app.nodes[0];const evidence=app.mcp.find(item=>item.for===entry.id);const items:PathItem[]=entry.hops.map(hop=>({id:hop.node_id,node:app.nodes.find(node=>node.id===hop.node_id)??app.nodes[0],label:hop.edge_label,caption:hop.caption}));return <section className={`workspace${inspectorOpen?'':' inspector-closed'}`}><aside className="journey-rail"><span className="panel-label">ENTRYPOINT</span><select className="entry-select" value={entryIndex} onChange={event=>{const next=Number(event.target.value);setEntryIndex(next);setHopId(app.entries[next]?.hops[0]?.node_id??'');onInspectorOpen();trackEvent('callpath_selected')}}>{app.entries.map((item,index)=><option value={index} key={item.id}>{item.label}</option>)}</select><div className="panel-label hops-label">PATH NODES <span>{entry.hops.length}</span></div>{entry.hops.map((hop,index)=><button key={`${index}-${hop.node_id}`} className={hopId===hop.node_id?'hop-row selected':'hop-row'} onClick={()=>{setHopId(hop.node_id);onInspectorOpen();trackEvent('callpath_hop_selected')}}><span className="hop-index">{String(index+1).padStart(2,'0')}</span><span><b>{hop.edge_label}</b><small>{hop.caption}</small></span></button>)}</aside><main className="main-panel"><div className="toolbar"><div><span className="panel-label">SELECTED REQUEST</span><h2>{entry.label}</h2></div><div className="toolbar-actions">{!inspectorOpen&&<button className="inspector-reopen" onClick={onInspectorOpen}>Show source</button>}<span className={`layout-source ${entry.hasLayout?'precomputed':'derived'}`}><i/>{entry.hasLayout?'Bundle layout':'Derived layout'}</span></div></div><PathCanvas items={items} selectedId={hopId} onSelect={id=>{setHopId(id);onInspectorOpen();trackEvent('callpath_hop_selected')}} points={entry.hops.map(hop=>hop.layout)} layoutSource={entry.hasLayout?'precomputed':'derived'}/><EvidencePanel evidence={evidence} fallbackTool="journey" fallbackArgs={entry.label} fallbackSummary={`${entry.hops.length} visible hops from the selected entrypoint.`} nodeCount={entry.hops.length}/></main>{inspectorOpen&&<NodeInspector node={selected} onClose={onInspectorClose}/>}</section>}
