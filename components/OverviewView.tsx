'use client'
import { useMemo, useState } from 'react'
import type { App, Node } from '../lib/lachesis'
import { Icon } from './Icon'
import { NodeInspector } from './NodeInspector'

type Mode='map'|'architecture'|'health'
type Props={app:App;onRecord:(action:string,target:string,detail:string)=>void}
const pos=(index:number)=>({x:92+(index%4)*178,y:66+Math.floor(index/4)*92})

function matches(node:Node,query:string,app:App){return query.trim().toLowerCase().split(/\s+/).filter(Boolean).every(term=>{const [key,...rest]=term.split(':');const value=rest.join(':');if(rest.length){if(key==='kind')return node.kind.includes(value);if(key==='file')return node.file.toLowerCase().includes(value);if(key==='has'&&value==='mcp')return app.mcp.some(item=>item.for===node.id);if(key==='edge')return app.edges.some(edge=>(edge.source===node.id||edge.target===node.id)&&(value==='alias'?edge.alias:value==='dynamic'?edge.dynamic:false))}return [node.id,node.label,node.file,node.kind].join(' ').toLowerCase().includes(term)})}

export function OverviewView({app,onRecord}:Props){
  const [query,setQuery]=useState('');const [mode,setMode]=useState<Mode>('map');const [selectedId,setSelectedId]=useState(app.nodes[0]?.id??'');const [inspectorOpen,setInspectorOpen]=useState(false)
  const visible=useMemo(()=>app.nodes.filter(node=>matches(node,query,app)),[app,query]);const visibleIds=new Set(visible.map(node=>node.id));const edges=app.edges.filter(edge=>visibleIds.has(edge.source)&&visibleIds.has(edge.target));const selected=app.nodes.find(node=>node.id===selectedId)??visible[0]??app.nodes[0]
  const flowCount=(nodeId:string)=>app.flows.filter(flow=>flow.steps.some(step=>step.node_id===nodeId)).length
  const entryCount=(nodeId:string)=>app.entries.filter(entry=>entry.hops.some(hop=>hop.node_id===nodeId)).length
  const chokePoints=app.nodes.map(node=>({node,flows:flowCount(node.id),entries:entryCount(node.id)})).filter(item=>item.flows+item.entries>0).sort((a,b)=>b.flows+b.entries-a.flows-a.entries).slice(0,6)
  const modules=[...new Set(app.nodes.map(node=>node.file||'Unknown file'))].map(file=>({file,nodes:app.nodes.filter(node=>(node.file||'Unknown file')===file)})).sort((a,b)=>b.nodes.length-a.nodes.length)
  const health=[{label:'Graph nodes',value:app.nodes.length},{label:'Relationships',value:app.edges.length},{label:'Value flows',value:app.flows.length},{label:'Request paths',value:app.entries.length},{label:'MCP records',value:app.mcp.length},{label:'Missing layouts',value:app.entries.filter(entry=>!entry.hasLayout).length}]
  function selectNode(id:string){const node=app.nodes.find(item=>item.id===id);setSelectedId(id);setInspectorOpen(true);if(node)onRecord('Inspected graph node',node.label||node.id,`${node.file}:${node.line}`)}
  const summary=selected?`${selected.label||selected.id} participates in ${flowCount(selected.id)} value flow${flowCount(selected.id)===1?'':'s'} and ${entryCount(selected.id)} request path${entryCount(selected.id)===1?'':'s'}. It has ${app.edges.filter(edge=>edge.source===selected.id||edge.target===selected.id).length} normalized relationship${app.edges.filter(edge=>edge.source===selected.id||edge.target===selected.id).length===1?'':'s'}.`:''
  const visibleIndex=(node:Node)=>visible.indexOf(node);const graphPos=(node:Node)=>pos(Math.max(0,visibleIndex(node)));const labelIndex=(node:Node)=>String(Math.max(0,visibleIndex(node))+1).padStart(2,'0')
  return <section className={`overview-workspace${inspectorOpen?'':' inspector-closed'}`}><main className="overview-main"><header className="overview-heading"><div><span className="context-kicker">SYSTEM MAP</span><h2>See the graph’s shape before following a path.</h2><p>Explore normalized relationships, shared choke points, module concentration, and bundle health from the evidence already present.</p></div><div className="overview-switch"><button className={mode==='map'?'active':''} onClick={()=>setMode('map')}><Icon name="target" size={13}/>Topology</button><button className={mode==='architecture'?'active':''} onClick={()=>setMode('architecture')}><Icon name="matrix" size={13}/>Architecture</button><button className={mode==='health'?'active':''} onClick={()=>setMode('health')}><Icon name="history" size={13}/>Health</button></div></header><div className="query-composer"><Icon name="search" size={15}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Ask the graph: kind:sink file:db/ edge:dynamic" aria-label="Filter graph nodes"/><div className="query-chips"><button onClick={()=>setQuery('kind:sink')}>sinks</button><button onClick={()=>setQuery('edge:dynamic')}>dynamic</button><button onClick={()=>setQuery('has:mcp')}>MCP</button></div></div>{mode==='map'&&<><div className="map-summary"><div><span>VISIBLE NODES</span><b>{visible.length}<small> / {app.nodes.length}</small></b></div><div><span>VISIBLE EDGES</span><b>{edges.length}<small> / {app.edges.length}</small></b></div><div><span>SELECTED</span><b>{selected?.kind||'—'}</b></div><p>{summary}</p></div><div className="topology-canvas"><svg viewBox="0 0 760 300" aria-hidden="true" focusable="false">{edges.map(edge=>{const source=visible.find(node=>node.id===edge.source),target=visible.find(node=>node.id===edge.target);if(!source||!target)return null;const a=graphPos(source),b=graphPos(target);const kind=edge.dynamic?'dynamic':edge.alias?'alias':'exact';return <path key={edge.id} className={`topology-edge ${kind}`} d={`M${a.x} ${a.y} C${(a.x+b.x)/2} ${a.y},${(a.x+b.x)/2} ${b.y},${b.x} ${b.y}`}/>})}{visible.map(node=>{const p=graphPos(node);return <g key={node.id} className={`topology-node kind-${node.kind}${selected?.id===node.id?' selected':''}`} onClick={()=>selectNode(node.id)}><circle cx={p.x} cy={p.y} r="24"/><text x={p.x} y={p.y+4} textAnchor="middle">{labelIndex(node)}</text><text className="topology-label" x={p.x} y={p.y+39} textAnchor="middle">{node.kind}</text></g>})}</svg><div className="topology-node-list" aria-label="Graph nodes">{visible.map(node=><button key={node.id} className={selected?.id===node.id?'selected':''} onClick={()=>selectNode(node.id)}><span>{labelIndex(node)}</span><b>{node.label||node.id}</b><small>{node.kind} · {node.file}:{node.line}</small></button>)}</div></div></>}{mode==='architecture'&&<div className="architecture-grid"><section><span className="panel-label">MODULE CONCENTRATION</span>{modules.map(module=><div className="module-row" key={module.file}><div><b>{module.file}</b><small>{module.nodes.length} nodes · {new Set(module.nodes.flatMap(node=>app.flows.filter(flow=>flow.steps.some(step=>step.node_id===node.id)).map(flow=>flow.id))).size} flows</small></div><span><i style={{width:`${Math.max(8,module.nodes.length/app.nodes.length*100)}%`}}/></span></div>)}</section><section className="choke-panel"><span className="panel-label">SHARED CHOKE POINTS</span><p>Nodes repeated across flows and requests. This is concentration evidence, not a risk score.</p>{chokePoints.map(item=><button key={item.node.id} onClick={()=>selectNode(item.node.id)}><span className={`kind-dot kind-${item.node.kind}`}/><b>{item.node.label||item.node.id}</b><small>{item.flows} flows · {item.entries} requests</small></button>)}</section></div>}{mode==='health'&&<div className="health-grid"><section className="health-metrics">{health.map(item=><div key={item.label}><span>{item.label}</span><b>{item.value}</b></div>)}</section><section className="health-notes"><span className="panel-label">BUNDLE DIAGNOSTICS</span>{app.edges.length?'Relationships are available for topology and convergence analysis.':'No explicit or path-derived relationships were found.'}{app.entries.some(entry=>!entry.hasLayout)&&<p><i/>Some request paths use derived layout coordinates.</p>}{app.nodes.some(node=>!node.file)&&<p><i/>Some nodes do not include a source file location.</p>}{!app.mcp.length&&<p><i/>No MCP evidence records are bundled; summaries remain derived.</p>}</section></div>}</main>{inspectorOpen&&selected&&<NodeInspector node={selected} onClose={()=>setInspectorOpen(false)}/>}</section>
}
