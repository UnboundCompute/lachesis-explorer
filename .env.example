# Leave empty when the build service is exposed through the same origin
# (for example, a reverse proxy at /api).
NEXT_PUBLIC_BUNDLE_API_URL=

# For a separate API deployment, use an HTTPS origin or path, for example:
# NEXT_PUBLIC_BUNDLE_API_URL=https://bundles.example.com
